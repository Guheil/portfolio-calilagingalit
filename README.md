# Patricia Llamado — Portfolio

This is a static portfolio website for Patricia Llamado (General Virtual Assistant).

- Built with HTML, CSS, and JavaScript
- Uses GSAP for animations and Font Awesome for icons

How to use:
1. Open `index.html` in a browser to preview
2. Serve locally via a static server (e.g., `python -m http.server` or `live-server`)

To connect a remote repository (GitHub/GitLab), add a remote URL and push:

```
# example
git remote add origin <remote-url>
git push -u origin main
```